
###############################################################################
# Asterisk Projects
###############################################################################

- project:
    name: 'testsuite'
    node: 'centos || debian'

    jobs:
        - python-jobs
        - testsuite-jobs

- project:
    name: 'TEST-asterisk'
    node: 'centos || debian'

    jobs:
        - asterisk-jobs

- project:
    name: 'asterisk'
    node: 'centos || debian'

    jobs:
        - asterisk-jobs

