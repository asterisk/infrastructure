
- job:
    name: gate-testsuite
    node: 'centos || debian'

    wrappers:
        - timeout:
            timeout: 60
            timeout-var: 'BUILD_TIMEOUT'
            fail: true
        - timestamps

    builders:
        - node-git-prep
        - gerrit-git-prep
        - shell: "/srv/git/infrastructure/jenkins/scripts/run-testsuite-unittests.sh" 

    publishers:
        - archive:
            artifacts: 'logs/*'

- job-group:
    name: testsuite-jobs
    jobs:
        - gate-testsuite


